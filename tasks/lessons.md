# Lessons

Append-only log of corrections made during agent sessions. Each entry teaches the next session how to avoid a specific mistake. Prune entries when the code they reference is refactored away.

Format: `YYYY-MM-DD — <one-line lesson>`. Add a second line with context if the lesson would be opaque without it.

---

_(no entries yet)_
